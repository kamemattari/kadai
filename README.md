## 2層ニューラルネットワークの構造

本実装では、入力層・隠れ層・出力層からなる
2層ニューラルネットワークを用いる。

入力 x はまず隠れ層に入力され、
その出力がさらに出力層へ入力される。

x → 隠れ層 h → 出力 y

## コード整理について

最初に動作確認のため最小構成の実装を行い、
その後、READMEでの説明と対応しやすくするため、
変数名や処理の区切りを整理した。

この整理により、隠れ層・出力層の役割や、
順伝播・誤差逆伝播の流れが分かりやすくなった。
